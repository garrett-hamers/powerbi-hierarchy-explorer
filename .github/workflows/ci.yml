name: CI

on:
  push:
    branches:
      - main
      - "garrett-hamers-*"
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: ESLint
        run: npm run eslint
      - name: Tests
        run: npm test
      - name: Package
        run: npm run package
      - name: Certification audit
        run: npm run certification-audit
      - name: Full dependency audit
        run: npm audit
